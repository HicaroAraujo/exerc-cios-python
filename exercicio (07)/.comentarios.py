
                                     #    COMENTARIOS PARA AJUDAR

# 10% = 0.10 , 5% = 0.5 , 15% = 0.15
# lista [] recebe um conjunto de dados 
# variavel.append -----> para colocar o itens para dentro da lista 
# pow(altura, 2) ------->>> para colocar ao quadrado
# math.sqrt(numero) ----->>>> raiz quadrada de um numero (para isso é nescessario import mach)
# imc = round(peso / pow(altura, 2), 2) -------> modifica a soma das variaveis antes de virar a variavel imc
# A função startswith(letra) é usada para verificar se a palavra começa com alguma letra:
# palavra[0]  ----->> variavel [] verifica a posião de cada caracter de uma lista, nome, palavra...
#  Se usarmos o método split() sem nenhum argumento, ele irá dividir a string em substrings onde encontrar espaços em branco. [10 11 12 ] >>> ["10", "11" , "12"]
# numeros.sort(reverse=True) ordena a lista numeros em ordem decrescente.
# ordem = sorted(numeros) >>> uma nova variavel recebe lista ordenada
# mesmo_tamanho = all(len(nome) == len(nomes[0]) for nome in nomes) >>>> verifica se todos os nome em uma lista nomes são iguais, só sai true se todos forem iguais



#--------------------------------------> if , elif, else, (condição)

# if letra.isupper(): verifica se  a variavel letra contem letra maiuscula
# if letra.isdigit():   # isdigit() ----> verifica se há numeros na variavel letra
# if len(senha) < 8:  verifica se a variavel senha tem pelo meno 8 caracteres.
# elif >>>>>> segunda condição.
# else >>>>> caso contario as outras condições.

# ------------------------------------>> while (quanto for verdade execute )
# break ------> sair do programa


# -------------------------------------> for (for variavel2 in variavel1) ---->>> (PARA CADA VARIAVEL NA VARIAVEL)

#  for --->  permite iterar sobre uma sequência (como uma lista, tupla, dicionário, conjunto ou string) e executar um bloco de código para cada elemento dessa sequência.
#  in  ---->  verifica a presença da letra  na senha
# range (1,6) ----> quantidade de vezes que o for vai funcionar

# -------------------------------------->
       
# ------------------------------------>  prints

# print(texto.upper())  # Transforma em maiúsculas
# print(texto.lower())  # Transforma em minúsculas

# print(type(variavel)) ----> verifica e print qual é o tipo da variavel 